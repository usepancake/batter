# Handoff — batter 0.6.0 audit + fixes → batter/MCP instance (2026-06-04)

## Kickoff prompt (paste this to start the instance)

> You are taking over **batter** (the deterministic backtest/verification engine,
> `pancake-engine-py`) and the **Pancake MCP tooling** (`pancake-production`). A
> 0.6.0 audit-and-fix pass just completed on branch `audit/batter-0.6.0` (annotated
> tag `0.6.0`, **292 tests green**, **NOT pushed or merged**; `main` untouched at
> `9ea7506`).
>
> **Read the rest of this file and re-verify everything skeptically before you act.**
> Do not trust the green test run blindly, and do **not** start the prod sequence
> (deploy + receipt re-hash) without Michael's explicit go.
>
> Your open decisions: (1) merge + push `audit/batter-0.6.0` + tag; (2) the gated
> prod sequence; (3) commit the still-uncommitted `pancake-production` (a)/DF-1 work.
> A mirror of this doc is in the vault at
> `projects/active/pancake/handoff-2026-06-04-batter-0.6.0-audit.md`.

---

## Context (what / where)
- **batter** = deterministic Python engine. `/Users/michaelmustopo/Documents/pancake-engine-py`
  (package `pancake_engine`, imported as `batter`). venv: `./.venv/bin/python`
  (**Python 3.12 only**). Tests: `./.venv/bin/python -m pytest -q` (**292 green**).
- **pancake-production** = `/Users/michaelmustopo/Documents/pancake-production` — TS
  orchestrator + engine HTTP shim (`engine/server.py`). Prod Supabase `qkllazozmuuuuxsjksey`.
- **`result_hash`** = SHA-256 over (compiled spec + `rows_sha256` + `engine_version`
  + config + every output metric) — the receipt's verifiable fingerprint.
  `ENGINE_VERSION` is *inside* the hash, so a version bump changes every hash by
  design (policy-B transparent: re-run + `old_hash→new_hash` record).

## What was done this session
1. **No-false-positive pass on the documented "gaps":** #3 (seed) & #4 (cutoff)
   were ALREADY fixed in `pancake-production/lib/evidence-runner/runner.ts` (false
   positives); (c) /tick stale; **DF-1 (gte/band paper 0 fills) is NOT an engine
   bug** — `tick()` opens gte/band correctly; root cause is the live feed
   `polymarket_gamma_bars_1d` having ~no high-priced *open* markets (407 bars, 3 at
   ≥0.7 one 0.999 market, 0 in [0.85,0.97]); the old "591 rows ≥0.7" was the
   HISTORICAL backtest pool. (a) rows_sha256 assert was REAL → fixed.
2. **Full audit of batter** (7 agents, every module, empirically probed): CORE —
   P&L, determinism, look-ahead, fills/cost/slippage, crypto-OHLCV, canonicalization
   — verified **already correct**. Findings = validation footguns + stats honesty +
   OSS polish, not P&L bugs.
3. **Cut batter 0.6.0** for the real findings (map below).

## Bug → fix map
| # | Bug (before) | Sev | Repo · file | Fix | Test |
|---|---|---|---|---|---|
| **#1** | typo'd/unknown operator (`gt`) or bare feature node → silently **always-true** ("enter on everything") | High | engine · `compile/condition.py` | reject unknown keys + require ≥1 of gte/lte/eq | `test_compile.py` |
| **#3-cond** | `feature_equal` both-absent → `True`; non-string keys silent | P1 | engine · `compile/condition.py` | both-present + string-key guards | `test_compile.py` |
| **#2** | permutation p can be exactly `0.0` | P1 | engine · `metrics/permutation.py` | `(count+1)/(n+1)` (Phipson & Smyth) | `test_audit_2026_06_fixes.py` |
| **#6** | degenerate zero-width bootstrap CI shown as `(0,0)` | P2 | engine · `metrics/bootstrap.py` | `(None,None)` + `BOOTSTRAP_INSUFFICIENT` | `test_audit…` |
| **#4** | dataset-declared range not enforced when spec omits it | P2 | engine · `validate/dataset.py` | enforce dataset range (TS parity) | `test_audit…` |
| **#7** | `n_resamples`/`n_permutations` unbounded (lib DoS) | P2 | engine · bootstrap/permutation | cap 1e6 | `test_audit…` |
| **#5** | running <3.12 silently changes `result_hash` | P2 | engine · `__init__.py` | import guard + `sum()` sentinel | `test_audit…` |
| **3a** | builtin `sum()` accumulation drifts across CPython versions | durability | engine · standard/permutation/pm/ledger | `math.fsum` | `test_audit…` |
| **(a)** | TS client took engine result verbatim (no `rows_sha256` check) | P1 | **prod** · `lib/evidence-runner/python-runner-client.ts` | boundary assert (engine recomputes the hash) | `tests/evidence-runner/python-runner-client.test.ts` |
| **DF-1** | gte/band paper 0 fills | not-a-bug | **prod** · `lib/paper-engine/dispatcher.ts` (observability) | `paper_tick_no_entries` log + engine regression tests | `dispatcher.test.ts` / engine `test_tick.py` |

## Engine state: before → now
- **Before:** an operator typo silently backtested "trade everything"; a strong
  signal could report `sharpe_p_value=0.0`; degenerate CIs printed as `(0,0)`;
  dataset-declared ranges ignored; `result_hash` stable only on 3.12; no resample
  cap; docstrings referenced a nonexistent private repo path.
- **Now (0.6.0):** malformed specs fail loud; `p ∈ [1/(n+1), 1]`, never 0; degenerate
  CI → `(None,None)`+warning; dataset ranges enforced; `result_hash` version-durable
  via `fsum`; caps in place; docstrings cleaned + `CHANGELOG.md`; tag `0.6.0`.
  **Core P&L / determinism / look-ahead / crypto unchanged — already correct.**

## State of the trees (uncommitted = loss risk)
- **pancake-engine-py**: branch `audit/batter-0.6.0`, commits `4af8971` (base) ·
  `92ed8fc` (fsum) · `aa0a0fc` (fold), tag `0.6.0`, 292 green, clean. **NOT pushed /
  merged.** `main` at `9ea7506`.
- **pancake-production**: **UNCOMMITTED (mine)** — `lib/evidence-runner/python-runner-client.ts`,
  `lib/paper-engine/dispatcher.ts`, `tests/evidence-runner/python-runner-client.test.ts`;
  plus a note added to the untracked `docs/FEATURE_DEBT.md` (another instance's file).
  `docs/TECH_DEBT.md` modified = NOT mine.
- **Vault**: `pancake-data-plane-architecture.md` has a "Verification update
  (2026-06-04)" block with all gap verdicts.

## Receipts (for the gated prod correction)
`backtest_results`: **247 rows, 2 owners, 0 public, all unlisted, minted under
`pancake-engine@0.1.x` (NOT 0.5.0).** → per-receipt correction = one internal
annotation; no external migration. Re-run the count to confirm.

## GATED — DO NOT START without Michael's explicit go
1. Deploy 0.6.0 to the Fly engine + bump the `Dockerfile.engine` pin.
2. Regenerate `pancake-production/tests/engine-acceptance/independent-rerun.test.ts`
   fixtures once against 0.6.0 → clears the RED pre-push (stale since the
   never-landed 0.5.0 re-hash).
3. Annotate the 247 internal receipts ("superseded by 0.6.0").
4. Anchor the deliberate-break note as an **amendment to the B-transparent ADR**.

## BE SKEPTICAL (Michael's explicit instruction)
- **Don't trust 292-green blindly** — my added tests assert the *new* behavior;
  confirm the new behavior is what you want.
- **DF-1 "not a bug" is time-dependent** — it hinges on the live feed lacking
  high-priced *open* markets now. Re-run the prod SQL; re-read `gamma-bar-reader.ts`.
- **#1 whitelist is a behavior change** — it rejects specs that were silently
  always-true. 292-green ≠ the whole user-spec space; confirm no live deployment
  relied on the old behavior before prod.
- **fsum moved the hash a third time within 0.6.0** — re-run the example fixtures +
  parity suite yourself.
- **Not probed:** the deployed Fly engine version (receipts say 0.1.x, shim docstring
  said 0.4.2 — confirm what's live); whether any unlisted receipt was shared
  externally; TS `metrics.ts` parity beyond the engine suite's 1e-9 runner check.
- **Deferred (P3, not done):** cosmetic "Engine 0.3" docstring/`--help` phrasing
  (broken *paths* fixed, dated phrasing left); `SIZING_CLIPPED` dead-path note;
  `parse_json` top-level export; CONTRIBUTING version-agnostic wording.

## Open / next
- Merge + push `audit/batter-0.6.0` → `main` + tag (Michael's call).
- Prod sequence (gated, above).
- Commit the pancake-production (a)/DF-1 work by name.
- Optional P3 OSS polish.
